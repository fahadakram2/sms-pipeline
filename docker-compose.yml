version: "3.9"

services:
  mongo:
    image: mongo:latest
    container_name: mongo
    ports:
      - 27017:27017
    volumes:
      - mongo_data:/data/db

  backend:
    image: pipeline-sms-backend
    container_name: backend
    ports:
      - 5000:5000
    depends_on:
      - mongo

  frontend:
    image: pipeline-sms-frontend
    container_name: frontend
    ports:
      - 3000:80
    depends_on:
      - backend

volumes:
  mongo_data:
